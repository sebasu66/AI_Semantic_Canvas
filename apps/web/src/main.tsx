import React from 'react';
import ReactDOM from 'react-dom/client';
import SpatialApp from './SpatialApp';
import './spatial.css';

ReactDOM.createRoot(document.getElementById('root')!).render(<React.StrictMode><SpatialApp /></React.StrictMode>);
